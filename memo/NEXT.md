# Next

## 発展機能追加

- Claude -> MCP Tool -> AIが的確な指示を生成（初回セットアップなどのツール呼び出し回数が多く、ぶれやすい箇所で導入）？
    - サブエージェントには Claude Codeを使う

- 動的なツールのだし分け・編集（FastMCPのMiddleware・Transformerを活用）

- まとめて問題・付随するクイズを作成する

## UI

- 文章もUIに表示するようにして、分からない単語や文章の難易度などを自動記録
- 分からない単語・文章を入力ボックスに入れて送信してAIに渡すあるいは自動でDB保存させる

## 既存機能改善

- 設定ファイルから、文章の長さなどを設定して、AIに弄らせる
    - AIがユーザーに質問、またはユーザーがAIに指示して、文章の長さなどの設定ファイルを変更する
    - Mcp Apps から操作できるように

- テスト機能追加
    - 今の物語形式の練習のみならず、自然対話形式、テスト形式での練習も可能にする
    - 専用UI

- クイズの回答モードを設定ファイルで管理して切り替えられるように

- 言語ごとにメモリ機能を追加
    - ちょっとした指示などをどんどん集める（とはいえ、これに頼ると肥大化するので、定期的に固定された指示に移すべき）

## 使い勝手

- UIのURLも表示するようにする
- Sqliteの中身を定期的に見れる専用UI作成？

## リファクタ

- 自由回答と選択式クイズの融合

## インフラ

- GCPへのデプロイ

## プロンプトなどのチューニング



## 設定値のチューニング

- `app_config.yaml` の `vocab_get_limit` を実際の使用感に合わせて調整