"""サービス層 (service)。

repository のデータアクセスの上に、ドメイン不変条件やネットワークを伴う
オーケストレーションを載せる:

- ``memo`` : セマンティック検索 (埋め込み呼び出し + ランキング)
- ``user`` : ユーザー CRUD のドメインルール (name 必須・trim・部分更新・
  admin 削除禁止)。認可 (admin 判定) は含まない。
"""
