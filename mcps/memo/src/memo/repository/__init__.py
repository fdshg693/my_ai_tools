"""データアクセス層 (repository)。

ドメインごとに SQL を分離する:

- ``memo`` : メモ (``memos`` テーブル) の CRUD・検索
- ``user`` : ユーザー台帳 (``users`` テーブル) の CRUD・登録判定

いずれも ``memo.database`` の接続ファクトリ (``_connect_db``) を使う純粋な
データアクセスで、認可 (どのユーザーが操作してよいか) は扱わない。
"""
