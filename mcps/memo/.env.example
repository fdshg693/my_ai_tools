# memo MCPサーバーの埋め込み実行に利用
OPENAI_API_KEY=YOUR_OPENAI_API_KEY_HERE

# admin に切り替えたとき user 管理ツール (admin タグ) を自動有効化するか。
# 既定は有効 (admin に切替で公開)。0/false/no/off にすると、admin に切り替えても
# 管理ツールは一切公開されない (危険な自動有効化の無効化スイッチ)。
MEMO_ADMIN_TOOLS_AUTO_ENABLE=false